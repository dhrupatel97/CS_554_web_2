<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Movie List</title>
    <link rel="stylesheet" href="/public/css/styles.min.css">
    <style>
        .grid {
            margin-top: 30px;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            grid-gap: 20px;
            margin-bottom: 30px;
        }

        .card-img-top {
            width: 100%;
            height: 15vh;
            object-fit: cover;
        }

        .card {
            box-shadow: 0 10px 9px rgba(252, 2, 2, 0.3);
            border-radius: 10px;
        }

        .card-title {
            font-weight: bold;
        }

        .card-body {
            text-align: center;
        }
    </style>
</head>

<body>
    {{{body}}}
    <script src="/public/js/vendor.min.js"></script>
</body>

</html>